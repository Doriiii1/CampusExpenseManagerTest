<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CampusExpenseManager"
        tools:targetApi="31">

        <!-- Login Activity (LAUNCHER) -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="false" />

        <!-- Sprint 2: Auth Activities -->
        <activity
            android:name=".activities.RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".activities.ProfileActivity"
            android:exported="false" />

        <!-- Sprint 3: Expense Activities -->
        <activity
            android:name=".activities.AddExpenseActivity"
            android:exported="false" />

        <activity
            android:name=".activities.ExpenseListActivity"
            android:exported="false" />

        <activity
            android:name=".activities.EditExpenseActivity"
            android:exported="false" />

        <!-- Sprint 4: Budget & Report Activities -->
        <activity
            android:name=".activities.SetBudgetActivity"
            android:exported="false" />

        <activity
            android:name=".activities.BudgetDashboardActivity"
            android:exported="false" />

        <activity
            android:name=".activities.EditBudgetActivity"
            android:exported="false" />

        <activity
            android:name=".activities.ReportActivity"
            android:exported="false" />

        <!-- Sprint 5: Income Activities -->
        <activity
            android:name=".activities.AddIncomeActivity"
            android:exported="false" />

        <activity
            android:name=".activities.IncomeListActivity"
            android:exported="false" />

        <activity
            android:name=".activities.EditIncomeActivity"
            android:exported="false" />

        <!-- FileProvider for Camera -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.campusexpensemanager.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

    </application>

</manifest>
    <!-- Compare this snippet from app/src/main/res/layout/activity_add_expense.xml: -->